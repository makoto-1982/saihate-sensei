{
  "name": "saihate-wall-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply saihate-wall-db --local",
    "db:remote": "wrangler d1 migrations apply saihate-wall-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
